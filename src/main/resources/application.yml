spring:
  datasource:
    url: jdbc:mysql://localhost:8111/fakenew?useSSL=false&serverTimezone=UTC
    username: root
    password:
  security: false

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
server:
  port: 8080

